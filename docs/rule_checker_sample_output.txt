==============================================================================
NetSage AI - Deterministic Rule Checker - Sample Execution
==============================================================================

--- Layer 2 VLAN pruning on a trunk ---
[FAIL] Trunk Allowed VLAN List (HIGH)
    evidence : Trunk allowed VLANs: '1-9,11-4094'.
    detail   : The trunk carries an explicit (pruned) allowed-VLAN list. Any production VLAN not in this range is dropped between switches.

--- Layer 3 duplicate IP conflict ---
[FAIL] Duplicate IP Address Detection (CRITICAL)
    evidence : %IP-4-DUPADDR: Duplicate address 192.168.10.1 on GigabitEthernet0/0
    detail   : IOS reports a duplicate IP (192.168.10.1) on GigabitEthernet0/0. Two devices claim the same address, causing ARP instability and intermittent loss.

--- Default gateway outside host subnet ---
[FAIL] Host / Gateway Subnet Containment (HIGH)
    evidence : Host 10.0.1.50/255.255.255.0 (network 10.0.1.0) does not contain gateway 10.0.2.1.
    detail   : The configured default gateway 10.0.2.1 is outside the host's local subnet, so ARP for the gateway never resolves.

--- DHCP relay missing (APIPA) ---
[FAIL] APIPA Address Allocation (HIGH)
    evidence : Host holds an APIPA link-local address 169.254.120.44.
    detail   : A 169.254.x.x address means the client never received a DHCP reply - the DHCP server is down or, across a routed boundary, 'ip helper-address' is missing.
[FAIL] DHCP Relay (ip helper-address) (HIGH)
    evidence : R1# show running-config interface g0/0.20
    detail   : The client subnet is on a routed subinterface with no 'ip helper-address', so broadcast DHCP Discover packets are never forwarded to the server.

--- Missing default route ---
[WARNING] Gateway of Last Resort (MEDIUM)
    evidence : 'Gateway of last resort is not set' with no default route present.
    detail   : No default route: traffic to any network not explicitly in the routing table is dropped. Add 'ip route 0.0.0.0 0.0.0.0 <next-hop>' or a routing-protocol default.

--- ACL denies DNS ---
[FAIL] ACL Denies DNS (UDP/53) (CRITICAL)
    evidence : 10 deny udp any host 10.50.1.10 eq domain (245 matches)
    detail   : An access-list entry explicitly denies UDP/53, so name resolution to that server fails while IP connectivity still works.
